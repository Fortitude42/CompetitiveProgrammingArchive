#include <bits/stdc++.h>
#include "testlib.h"
using namespace std;

string s;
int k;

int main (int argc, char* argv[]) {
    registerValidation(argc, argv);
	string s = inf.readToken(format("[(a-z|?)]{%d}", n), "s");
}