#include <bits/stdc++.h>
using namespace std;
using ll = long long;
const int N = 2e5 + 5, inf = 2e9;
int main() {
	ios :: sync_with_stdio(0), cin.tie(0), cout.tie(0);
	int n;
	cin >> n;
	string s = "Hello, World!\n";
	assert(n >= 0);
	int sz = (int)s.size();
	if (n < sz)
		cout << s[n];
	return 0;
}



